{% set name = "vyper" %}
{% set version = "0.4.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/vyper-{{ version }}.tar.gz
  sha256: 2a219b895c9b5ad6a71237a6fb7d03a6eccaa800faa30ba32418847e350a95e3

build:
  number: 0
  skip: true  # [py2k or py<38 or py>=311]
  entry_points:
    - vyper=vyper.cli.vyper_compile:_parse_cli_args
    - vyper-serve=vyper.cli.vyper_serve:_parse_cli_args
    - vyper-ir=vyper.cli.vyper_ir:_parse_cli_args
    - vyper-json=vyper.cli.vyper_json:_parse_cli_args
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - pytest-runner
    - python
    - setuptools-scm
  run:
    - asttokens ==2.0.5
    - pycryptodome >=3.5.1,<4
    - python
    - semantic_version >=2.10,<3
    - wheel

test:
  imports:
    - vyper
  commands:
    - pip check
    - vyper --help
    - vyper-serve --help
    - vyper-ir --help
    - vyper-json --help
  requires:
    - pip

about:
  home: https://github.com/vyperlang/vyper
  summary: 'Vyper: the Pythonic Programming Language for the EVM'
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - spinoch
    - fubuloubu
    - iamdefinitelyahuman
